{
    "version": 2,
    "builds": [
        {
            "src": "build.sh",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "output"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dashboard.py"
        }
    ]
}
